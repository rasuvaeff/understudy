{
    "name": "understudy-family-docs",
    "private": true,
    "type": "module",
    "scripts": {
        "docs:api": "node scripts/generate-api.mjs",
        "docs:migration": "node scripts/render-migration.mjs",
        "docs:check": "node scripts/check-integrity.mjs",
        "docs:check:anchors": "node scripts/check-anchors.mjs",
        "docs:check:cookbook": "node scripts/check-cookbook.mjs",
        "docs:check:drafts": "node scripts/check-drafts.mjs",
        "docs:check:links": "node scripts/check-links.mjs",
        "docs:check:migration": "node scripts/render-migration.mjs --check",
        "docs:dev": "npm run docs:api && vitepress dev src",
        "docs:build": "npm run docs:check:drafts && npm run docs:api && npm run docs:check:migration && npm run docs:check && vitepress build src && npm run docs:check:anchors",
        "docs:preview": "vitepress preview src"
    },
    "devDependencies": {
        "markdown-link-check": "^3.15.0",
        "vitepress": "^1.6.4"
    }
}
